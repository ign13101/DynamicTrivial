<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DynamicTrivial"
        tools:targetApi="31">

        <activity android:name=".StartupActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">

        </activity>

        <activity
            android:name=".RegisterActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">
        </activity>

        <activity
            android:name=".DiceSensorActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">

        </activity>

        <activity
            android:name=".DiceActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">
        </activity>

        <activity
            android:name=".OrderActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">

        </activity>
        <activity
            android:name=".DondeCaiActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">

        </activity>
        <activity
            android:name=".PreguntaActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">

        </activity>
        <activity
            android:name=".ResultadoActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">

        </activity>
        <activity
            android:name=".ResumenActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">

        </activity>
        <activity
            android:name=".ClasificacionActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">

        </activity>
        <activity
            android:name=".NuevoJugadorActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">

        </activity>
        <activity
            android:name=".VictoryActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DynamicTrivial">

        </activity>
    </application>

</manifest>